# DataSource settings: set here your own configurations for the database 
# connection. In this example we have "netgloo_blog" as database name and 
# "root" as username and password.
spring.datasource.url=jdbc:mysql://rm-bp15l5d0911010v33o.mysql.rds.aliyuncs.com:3306/idp?autoReconnect=true&useSSL=false
spring.datasource.username=zongmu
spring.datasource.password=Initial1
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# Keep the connection alive if idle for a long time (needed in production)
spring.datasource.testWhileIdle=true
spring.datasource.validationQuery=SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql=true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto=update

# Naming strategy
spring.jpa.hibernate.naming-strategy=org.hibernate.cfg.ImprovedNamingStrategy
spring.jpa.properties.jadira.usertype.autoRegisterUserTypes = true

# Use spring.jpa.properties.* for Hibernate native properties (the prefix is
# stripped before adding them to the entity manager)

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL5InnoDBDialect

# Disable default security password
security.basic.enabled=false 
management.security.enabled=false

#security.oauth2.client.clientId=1234
#security.oauth2.client.clientSecret=1234

#logging.level.org.springframework=DEBUG

#spring.redis.host=10.58.118.60
spring.redis.host=localhost
spring.redis.password=
spring.redis.port=6379
#spring.session.store-type=redis
server.session.timeout=36000

server.session.timeout=5000
spring.session.timeout=10000

server.port=4033
#spring.thymeleaf.cache=false
#spring.thymeleaf.prefix=classpath:/templates
#spring.thymeleaf.mode=LEGACYHTML5

spring.profiles.active=Development
